#include <arpa/inet.h>
#include <netinet/in.h>
#include <stdio.h>
#include <sys/types.h>
#include <sys/socket.h>
#include <unistd.h>

#define BUFLEN 512 // taille buffer
#define NPACK 10 // nb de paquets
#define PORT 31469
#define SRV_IP "10.5.7.3"

void diep(char *s){
  perror(s);
  exit();
}

int main(int argc, char ** argv){
  struct sockaddr_in serv;
  int s, i, slen=sizeof(serv);//somme des tailles des attributs
  char buf[BUFLEN];

